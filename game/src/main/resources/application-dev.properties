# Development configuration
spring.application.name=game
spring.profiles.active=dev

# MongoDB configuration for dev
spring.data.mongodb.uri=mongodb://localhost:27017/spygame_dev

# Server configuration
server.port=8080
